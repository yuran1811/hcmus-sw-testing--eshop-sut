# Hướng dẫn điền session P01–P07

1. Copy cấu trúc từ một file `P0x.md` (đã có sẵn).
2. Điền **trong hoặc ngay sau** phiên thật — không bịa.
3. Outcome chỉ một trong: `SUCCESS_UNASSISTED` | `SUCCESS_ASSISTED` | `FAIL` | `ABANDONED`.
4. Dùng đúng định nghĩa Error / Wrong turn / Hesitation / Intervention như trong `../pilot/P00.md`.
5. SUS + probes theo `../../01-plan/instruments.md`.
6. Recording (nếu có) đặt vào `../evidence/` tên `P0x-recording.*` (không commit file quá lớn nếu Git cấm — ghi link Drive thay thế).
