### Session — {TOOL_NAME} — {DATE}

**AI Tool:** {tool name + model}
**Date/Time:** {YYYY-MM-DD HH:MM}

**Prompt:**
> {verbatim prompt text}

**AI Output:**
> {verbatim output — or: "See `{path/to/artifact}` (commit `{hash}`) — unedited AI output" / "See `{path}`, then manually corrected: {what you changed and why}"}
