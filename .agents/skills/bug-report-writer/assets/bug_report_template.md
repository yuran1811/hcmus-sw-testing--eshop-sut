# BUG-XX: <Tiêu đề ngắn gọn mô tả lỗi>

**API / Endpoint:** `<METHOD> /path`
**FR liên quan:** FR-xx
**Test_ID liên quan:** <Test_ID trong file Excel>
**Severity:** Critical / High / Medium / Low
**Phát hiện bởi:** AI-suggested test case đã audit / Human (Extend step)

## Mô tả
<Mô tả ngắn gọn hành vi sai lệch so với spec>

## Các bước tái hiện (Steps to Reproduce)
1. ...
2. ...
3. ...

## Request gửi đi
```http
<METHOD> <URL>
X-Student-Id: <StudentID>
Content-Type: application/json

<body nếu có>
```

## Kết quả mong đợi (theo spec)
<Trích hoặc diễn giải đúng theo api_specification.md>

## Kết quả thực tế
```json
<response thực tế nhận được, kèm status code>
```

## Ảnh chụp màn hình
<Đính kèm ảnh Postman/Newman report ở đây>

## Tác động
<Ảnh hưởng gì tới người dùng/hệ thống nếu không sửa — đặc biệt nêu rõ nếu liên quan bảo mật>

## Đề xuất
<Gợi ý hướng sửa nếu có thể>
