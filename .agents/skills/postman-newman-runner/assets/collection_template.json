{
  "info": {
    "name": "HW06 - EShop API Tests",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "event": [
    {
      "listen": "prerequest",
      "script": {
        "type": "text/javascript",
        "exec": [
          "const studentId = pm.environment.get('studentId');",
          "if (!studentId) {",
          "    console.warn('[X-Student-Id] Environment variable \"studentId\" is not set');",
          "} else {",
          "    pm.request.headers.upsert({ key: 'X-Student-Id', value: studentId });",
          "    console.log('[X-Student-Id] Header set = ' + studentId + ' | Request: ' + pm.info.requestName);",
          "}"
        ]
      }
    }
  ],
  "item": [
    {
      "name": "Pool A - Auth",
      "item": [
        {
          "name": "TC-A-001 Login success (VALID)",
          "request": {
            "method": "POST",
            "header": [{ "key": "Content-Type", "value": "application/json" }],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"email\": \"valid.user@example.com\",\n  \"password\": \"CorrectPass123!\"\n}"
            },
            "url": "{{baseUrl}}/auth/login"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('Status code is 200', function () { pm.response.to.have.status(200); });",
                  "const body = pm.response.json();",
                  "pm.test('Response contains token', function () { pm.expect(body).to.have.property('token'); });",
                  "if (body.token) { pm.environment.set('authToken', body.token); }",
                  "pm.test('Password is not exposed in response', function () {",
                  "    pm.expect(JSON.stringify(body)).to.not.include('password');",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "TC-A-002 Login with invalid password (VALID - negative)",
          "request": {
            "method": "POST",
            "header": [{ "key": "Content-Type", "value": "application/json" }],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"email\": \"valid.user@example.com\",\n  \"password\": \"wrongpass\"\n}"
            },
            "url": "{{baseUrl}}/auth/login"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('Status code is 401', function () { pm.response.to.have.status(401); });"
                ]
              }
            }
          ]
        }
      ]
    }
  ]
}
